# 开发与提交约定

- 仓库保持私有。
- 远端维护 `main` 与 `1.20.1` 两条分支。
- 开发过程保留在本地。功能开发完成并通过相应验证后，将整项功能整理为一个汇总提交合入 `main`。
- 不向远端上传本地开发分支及其过程提交，不使用 `--mirror`、`--all` 或自动推送标签。
- 移植版本按对应 Minecraft 与加载器环境单独验证。
- 保留各项目许可证及必要署名，不把本机凭据、个人联系方式或开发环境日志加入提交。
