package com.moakiee.thunderbolt.core.keys;

import net.minecraft.core.component.DataComponentMap;

/** Internal read-only component view; no ownership is transferred to callers. */
public interface FluidKeyComponents {
    DataComponentMap thunderbolt$components();
}
